<div class="max-w-4xl mx-auto py-8 px-4">
  <div class="mb-8">
    <h1 class="text-3xl font-bold text-gray-900">New Printable</h1>
    <p class="text-gray-600 mt-2">Create a new printable PDF resource</p>
  </div>

  <%= render 'form', printable: @printable %>
</div>
