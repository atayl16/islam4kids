<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Islam4Kids - Inspiring Islamic Education for Children" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="description" content="A safe, engaging platform for young Muslims to learn, grow, and discover the beauty of Islam through stories and educational content.">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="min-h-screen bg-gradient-to-br from-slate-50 via-emerald-50/30 to-violet-50/30">
    <%= render "shared/navbar" %>

    <!-- Flash Messages -->
    <% if notice.present? %>
      <div class="fixed top-24 right-4 z-40 max-w-md animate-slide-in">
        <div class="bg-emerald-500 text-white px-6 py-4 rounded-xl shadow-2xl flex items-start space-x-3" role="alert">
          <svg class="w-6 h-6 flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
          </svg>
          <p class="flex-1 font-medium"><%= notice %></p>
        </div>
      </div>
    <% end %>
    <% if alert.present? %>
      <div class="fixed top-24 right-4 z-40 max-w-md animate-slide-in">
        <div class="bg-red-500 text-white px-6 py-4 rounded-xl shadow-2xl flex items-start space-x-3" role="alert">
          <svg class="w-6 h-6 flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
          </svg>
          <p class="flex-1 font-medium"><%= alert %></p>
        </div>
      </div>
    <% end %>

    <main class="container mx-auto mt-28 px-5 py-8 min-h-screen">
      <%= yield %>
    </main>

    <!-- Footer -->
    <footer class="bg-gradient-to-r from-slate-800 via-emerald-900 to-violet-900 text-white py-12 mt-16">
      <div class="container mx-auto px-5">
        <div class="grid md:grid-cols-3 gap-8">
          <!-- Brand -->
          <div>
            <div class="flex items-center space-x-3 mb-4">
              <div class="w-10 h-10 bg-gradient-to-br from-emerald-400 to-violet-400 rounded-lg flex items-center justify-center">
                <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13C19.832 18.477 18.247 18 16.5 18c-1.746 0-3.332.477-4.5 1.253"></path>
                </svg>
              </div>
              <span class="text-2xl font-bold">Islam4Kids</span>
            </div>
            <p class="text-emerald-100 text-sm leading-relaxed">
              Inspiring Islamic education for children through engaging stories and educational content.
            </p>
          </div>

          <!-- Quick Links -->
          <div>
            <h3 class="text-lg font-bold mb-4 text-emerald-300">Explore</h3>
            <ul class="space-y-2">
              <li><%= link_to "Educational Blogs", blogs_path, class: "text-emerald-100 hover:text-white transition-colors" %></li>
              <li><%= link_to "Inspiring Stories", stories_path, class: "text-emerald-100 hover:text-white transition-colors" %></li>
              <li><%= link_to "Home", root_path, class: "text-emerald-100 hover:text-white transition-colors" %></li>
            </ul>
          </div>

          <!-- Contact -->
          <div>
            <h3 class="text-lg font-bold mb-4 text-emerald-300">Connect</h3>
            <p class="text-emerald-100 text-sm mb-4">
              Building a foundation of faith, one story at a time.
            </p>
            <p class="text-emerald-100 text-sm">
              &copy; <%= Time.current.year %> Islam4Kids. All rights reserved.
            </p>
          </div>
        </div>
      </div>
    </footer>
  </body>
</html>
